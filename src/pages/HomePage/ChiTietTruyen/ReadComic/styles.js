import { StyleSheet, Dimensions } from "react-native";

const { width: winWidth, height: winHeight } = Dimensions.get('window');

const StylesReadMic = StyleSheet.create({
   
})


export default StylesReadMic;